# Pipelines

Project-specific pipeline definitions for the scientific assistant live here.

Add YAML pipeline manifests (e.g., `tool_training.yaml`) that orchestrate data preparation, training, evaluation, and deployment steps using the core NexaCompute runners.

